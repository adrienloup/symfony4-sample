{% extends 'base.html.twig' %}
{% block title %}Bracelets (Jewelry){% endblock %}
{% block body %}
<main>
  <h1 class="title">The bracelets</h1>
  <div class="jewelry">
    {% for jewel in bracelets %}
    <div class="jewel">
      <a href="{{ path('client.jewelry.single', { id: jewel.id, slug: jewel.slug }) }}" class="jewel-inside">
        <h2 class="jewel-title">{{ jewel.name }} - {{ jewel.category }}</h2>
        <strong class="jewel-price">{{ jewel.formattedPrice }} €</strong>
        <span class="jewel-reference">ref {{ jewel.reference }}</span>
      </a>
    </div>
    {% endfor %}
  </div>
</main>
{% endblock %}
